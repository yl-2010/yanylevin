{
  "name": "yanylevin-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Yan Levin personal-site Express API (LM Studio GPT-OSS via Cloudflare Tunnel)",
  "scripts": {
    "start": "node --env-file=.env index.js",
    "dev": "node --env-file=.env --watch index.js",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "jose": "^6.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
